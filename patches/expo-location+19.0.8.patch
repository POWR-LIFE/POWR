diff --git a/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt b/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
index 80647ab..972930d 100644
--- a/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
+++ b/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
@@ -88,8 +88,16 @@ class LocationTaskService : Service() {
       builder.setContentIntent(contentIntent)
     }
 
+    // POWR: use the app's notification silhouette (generated by the expo-notifications
+    // config plugin as @drawable/notification_icon) so the foreground-service banner
+    // matches our push notifications, instead of the full-color launcher icon — which
+    // Android renders as a white blob when used as a small icon. Resolved by name at
+    // runtime to avoid a compile-time R dependency; falls back to the launcher icon.
+    val smallIconId = resources.getIdentifier("notification_icon", "drawable", packageName)
+      .takeIf { it != 0 } ?: applicationInfo.icon
+
     return builder.setCategory(Notification.CATEGORY_SERVICE)
-      .setSmallIcon(applicationInfo.icon)
+      .setSmallIcon(smallIconId)
       .build()
   }
 
diff --git a/node_modules/expo-location/ios/TaskConsumers/EXGeofencingTaskConsumer.m b/node_modules/expo-location/ios/TaskConsumers/EXGeofencingTaskConsumer.m
index 7e6482e..be2ed5e 100644
--- a/node_modules/expo-location/ios/TaskConsumers/EXGeofencingTaskConsumer.m
+++ b/node_modules/expo-location/ios/TaskConsumers/EXGeofencingTaskConsumer.m
@@ -71,8 +71,12 @@ - (void)startMonitoringRegionsForTask:(id<EXTaskInterface>)task
     self->_regionStates = regionStates;
 
     locationManager.delegate = self;
-    locationManager.allowsBackgroundLocationUpdates = YES;
-    locationManager.pausesLocationUpdatesAutomatically = NO;
+    // NOTE: allowsBackgroundLocationUpdates is NOT set here.
+    // Region monitoring (geofencing) is handled by the OS and does not require
+    // continuous background location access. Setting this flag requires the
+    // "location" UIBackgroundModes key, which Apple rejects under Guideline 2.5.4
+    // unless the app uses persistent real-time location. Region monitoring works
+    // correctly without it — the OS wakes the app on boundary transitions.
 
     for (NSDictionary *regionDict in regions) {
       NSString *identifier = regionDict[@"identifier"] ?: [[NSUUID UUID] UUIDString];
